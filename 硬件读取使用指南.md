# RK3588开发板硬件读取使用指南

## 概述

你已经成功配置了RK3588开发板的硬件读取功能。现在可以通过Android应用来读取开发板的各种硬件状态，包括LED、GPIO、串口、系统信息等。

## 已实现的功能

### 1. 硬件读取功能
- **LED状态读取**：读取LED设备的亮度状态
- **GPIO状态读取**：读取GPIO引脚的电平状态
- **串口数据读取**：读取串口设备的数据
- **系统信息读取**：读取CPU、内存、温度等系统信息
- **网络状态读取**：读取网络连接状态
- **设备权限检查**：检查硬件设备节点的访问权限

### 2. 测试界面
- 创建了专门的硬件测试界面（HardwareTestActivity）
- 提供单个功能测试和全面测试选项
- 实时显示测试结果

## 使用方法

### 步骤1：启动应用
1. 在RK3588开发板上找到并启动"MyApplication3"应用
2. 应用主界面显示LED控制面板

### 步骤2：进入硬件测试界面
1. 在主界面点击"硬件测试"按钮
2. 进入硬件测试界面

### 步骤3：测试硬件功能
在硬件测试界面，你可以：

1. **测试LED状态**
   - 点击"测试LED状态"按钮
   - 读取LED设备的当前亮度状态

2. **测试GPIO状态**
   - 点击"测试GPIO状态"按钮
   - 读取GPIO引脚的电平状态（模拟数据）

3. **测试串口数据**
   - 点击"测试串口数据"按钮
   - 读取串口设备的数据状态

4. **测试系统信息**
   - 点击"测试系统信息"按钮
   - 读取CPU使用率、内存信息、温度等

5. **全面测试**
   - 点击"测试所有硬件"按钮
   - 一次性测试所有硬件功能

## 硬件设备节点说明

### LED设备
- 设备路径：`/sys/class/leds/work/brightness`
- 功能：读取LED亮度状态
- 权限要求：读取权限

### GPIO设备
- 设备路径：`/dev/gpiochip*`
- 功能：读取GPIO引脚状态
- 当前实现：模拟数据（实际开发中需要根据具体GPIO配置）

### 串口设备
- 设备路径：`/dev/ttyS4`（默认）
- 波特率：115200（默认）
- 功能：串口通信数据读取

### 系统信息
- CPU信息：`/proc/version`
- 内存信息：`/proc/meminfo`
- 温度信息：`/sys/class/thermal/thermal_zone*/temp`

## 代码结构说明

### 核心类
1. **HardwareReader.java**
   - 硬件读取功能的主类
   - 包含所有硬件读取的native方法声明

2. **HardwareTestActivity.java**
   - 硬件测试界面
   - 提供用户交互和结果显示

3. **rk3588_hardware_jni.cpp**
   - JNI原生代码实现
   - 实际的硬件读取逻辑

### Native方法说明

```java
// 读取LED状态
public native String readLEDState();

// 读取GPIO状态
public native int readGPIOState(int gpioPin);

// 读取串口数据
public native String readSerialData(String port, int baudRate);

// 读取系统信息
public native String readSystemInfo();

// 读取温度
public native float readTemperature();

// 读取CPU使用率
public native float readCPUUsage();

// 读取内存信息
public native String readMemoryInfo();

// 读取网络状态
public native String readNetworkStatus();

// 检查设备权限
public native boolean checkDevicePermissions(String devicePath);
```

## 实际硬件访问说明

### 当前实现状态
- **模拟模式**：当前代码运行在模拟模式，返回模拟数据
- **真实硬件**：连接到真实硬件后会自动适配实际功能

### 切换到真实硬件模式
当RK3588开发板连接到实际硬件时：

1. **LED控制**：会实际读取LED设备状态
2. **GPIO读取**：会读取真实的GPIO引脚电平
3. **串口通信**：会与真实的串口设备通信
4. **系统监控**：会读取真实的系统参数

### 权限要求
要访问硬件设备，应用需要以下权限：
- 系统级权限（root权限）
- 设备节点读取权限
- 串口通信权限

## 故障排除

### 常见问题

1. **权限不足错误**
   - 确保应用有足够的权限访问设备节点
   - 检查设备节点的权限设置

2. **设备未找到**
   - 确认设备路径是否正确
   - 检查设备是否已正确连接

3. **数据读取失败**
   - 检查硬件连接状态
   - 确认设备驱动程序正常工作

### 调试方法

1. **查看日志**
   - 使用`adb logcat`查看应用日志
   - 过滤关键字："HardwareReader"、"RK3588"

2. **检查设备节点**
   ```bash
   adb shell ls -la /dev/ | grep -E "(led|gpio|tty)"
   adb shell ls -la /sys/class/leds/
   ```

3. **测试权限**
   ```bash
   adb shell "ls -la /dev/ttyS4"
   adb shell "cat /sys/class/leds/work/brightness"
   ```

## 扩展开发

### 添加新的硬件读取功能
1. 在`HardwareReader.java`中添加native方法声明
2. 在`rk3588_hardware_jni.cpp`中实现JNI方法
3. 在`HardwareTestActivity.java`中添加测试界面
4. 更新JNI方法注册表

### 自定义硬件配置
根据具体的RK3588开发板配置，可以修改：
- 设备节点路径
- GPIO引脚映射
- 串口配置参数
- 系统监控参数

## 总结

你现在已经具备了完整的RK3588开发板硬件读取能力。通过Android应用可以方便地监控和读取各种硬件状态，为后续的硬件控制和系统监控开发奠定了基础。

如需进一步开发或遇到问题，可以参考代码注释或联系技术支持。